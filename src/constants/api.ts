// Backend origin. Override at runtime via `window.API_BASE = "https://your-api"`
// in a small inline <script> before the app's module script, or edit the
// default below.
export const API_BASE = window.API_BASE || "http://localhost:8000";
